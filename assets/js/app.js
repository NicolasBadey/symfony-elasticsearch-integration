import 'jquery';
import 'popper.js';
import 'bootstrap';

require('../css/app.scss');

if (process.env.NODE_ENV !== 'production') {
    // Dev-only stuff goes here and will not be visible when Webpack compile in production mode
}